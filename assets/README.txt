This build can't reach the internet, so no real video files were added here.

Drop your own clips in this folder with these exact names and every sound
button on the site will start working immediately:

  assets/hero-ai.mp4   -> used on: Home hero, Skills page banner, Journey page banner, Contact page banner
  assets/brain.mp4      -> used on: About page banner, Projects page + listing, Blog page banner

Until the files exist, the ambient background video areas simply stay empty
(everything else on the page still works normally).
